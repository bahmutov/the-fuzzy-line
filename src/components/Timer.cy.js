import React from 'react'
import { Timer } from './Timer'
import '../App.css'

describe('Timer', () => {
  it('sets the clock to the given value', () => {
    // set the time the game has started
    // to 15 minutes before now
    // pass the time via context provider
    // when mounting the timer
    // Check the page shows "15:00"
  })
})
