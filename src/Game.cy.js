/// <reference types="cypress" />
import React from 'react'
import './App.css'
import { Game } from './Game'

it('plays the game', () => {
  // mount the Game component and pass the initial
  // and solved arrays as props.
  // finish the game by filling the empty cells
  // at the end, th game should show the win overlay
})
