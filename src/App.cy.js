/// <reference types="cypress" />
import React from 'react'
import './App.css'
import { App } from './App'

it('stubs the ES6 import', () => {
  // mount the app component
  // there should be filled and empty cells
})
